# pnpm / npm install output — never commit
node_modules/

# Vite cache (project root only — dist/.vite/manifest.json is committed)
/.vite/

# Logs
*.log
.pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
*.swp

# Agents
.agents/
.cursor/
.claude/
# Hero video masters and encode outputs — these live on the server, not in git
/build/
*.mov
/as\ *.mp4
/as\ *.webm
