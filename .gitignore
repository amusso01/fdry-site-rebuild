# pnpm / npm install output — never commit
node_modules/

# Vite cache (project root only — dist/.vite/manifest.json is committed)
/.vite/

# Logs
*.log
.pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
*.swp

# Agents
.agents/
.cursor/
.claude/
# Hero video masters (build/masters/) and encode outputs (build/video/) — these live on the server, not in git
/build/
# Full showreel (FDRY_SHOWREEL_BASENAME) — deployed to the server by hand, see video-encode.md
/media/*.mp4
*.mov
